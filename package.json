{
  "name": "web-portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "build-scss": "sass-convert index.scss index.sass && sass index.sass index.css",
    "watch-scss": "npm run build-scss && node-sass-chokidar src/ -o src/ --watch",
    "build-coffee": "coffee util.coffee -o util.js",
    "watch-coffee": "npm run build-coffee && coffee -w util.coffee -o util.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
